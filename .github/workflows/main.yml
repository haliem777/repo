name: first_repo

on:
     push:


       branches:

       - main

jobs:
   ex_job:
    runs-on: ubuntu-latest
    steps:
    - name: print a welcome message
      run: echo "wlcome to our world"
    
    
